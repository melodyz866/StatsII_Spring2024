# Global null hypothesis test
anova(model, test="Chisq")
# load data
load(url("https://github.com/ASDS-TCD/StatsII_Spring2024/blob/main/datasets/climateSupport.RData?raw=true"))
model <- glm(choice ~ countries + sanctions, data=climateSupport, family=binomial(link="logit"))
# Display the summary of the model
summary(model)
View(model)
# Global null hypothesis test
anova(model, test="Chisq")
# summary of the model
model_summary<-summary(model)
# Global null hypothesis test
anova_file<-anova(model, test="Chisq")
View(anova_file)
View(model_summary)
sink()
sink()
# load data
load(url("https://github.com/ASDS-TCD/StatsII_Spring2024/blob/main/datasets/climateSupport.RData?raw=true"))
model <- glm(choice ~ countries + sanctions, data=climateSupport, family=binomial(link="logit"))
# Fit the model
model <- glm(choice ~ countries + sanctions, data=climateSupport, family=binomial(link="logit"))
# Get the summary of the model
model_summary <- summary(model)
# Conduct the ANOVA test
model_anova <- anova(model, test="Chisq")
# Save the summary to a text file
summary_file <- "model_summary.txt"
sink(summary_file)
print(model_summary)
sink()  # This stops the sink and should be paired with the sink call above
# Save the ANOVA results to a text file
anova_file <- "model_anova.txt"
sink(anova_file)
print(model_anova)
sink()  # This stops the sink and should be paired with the sink call above
View(model_anova)
coef_Q <- -0.009950
intercept <- -0.005665
coef_L <- 0.458452
coef_Q <- -0.009950
L_value <- 80 / 192
Q_value <- L_value^2
log_odds = intercept + (coef_L * L_value) + (coef_Q * Q_value)
# Convert log-odds to probability
probability = exp(log_odds) / (1 + exp(log_odds))
print(probability)
model_with_interaction <- glm(choice ~ countries * sanctions, family=binomial(link="logit"), data=climateSupport)
anova(model, model_with_interaction, test="Chisq")
